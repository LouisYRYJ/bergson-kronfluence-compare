diff --git a/kronfluence/analyzer.py b/kronfluence/analyzer.py
index 615c1c0..ea1918c 100644
--- a/kronfluence/analyzer.py
+++ b/kronfluence/analyzer.py
@@ -39,7 +39,7 @@ def prepare_model(
         params.requires_grad = False
     for buffers in model.buffers():
         buffers.requires_grad = False
-
+    
     # Install `TrackedModule` wrappers on supported modules.
     model = wrap_tracked_modules(model=model, task=task)
     return model
diff --git a/kronfluence/arguments.py b/kronfluence/arguments.py
index a5ae0c3..e2506a0 100644
--- a/kronfluence/arguments.py
+++ b/kronfluence/arguments.py
@@ -64,6 +64,14 @@ class FactorArguments(Arguments):
         metadata={"help": "Indicates whether shared parameters are present in the model's forward pass."},
     )
 
+    include_bias: bool = field(
+        default=True,
+        metadata={
+            "help": "If `True`, appends a ones column to activations to account for bias terms "
+            "in linear layers. Set to `False` to exclude bias from covariance computation."
+        },
+    )
+
     # Configuration for fitting covariance matrices. #
     covariance_max_examples: Optional[int] = field(
         default=100_000,
diff --git a/kronfluence/factor/covariance.py b/kronfluence/factor/covariance.py
index b32ee04..da038a8 100644
--- a/kronfluence/factor/covariance.py
+++ b/kronfluence/factor/covariance.py
@@ -1,3 +1,4 @@
+import os
 from pathlib import Path
 from typing import Dict, List, Optional, Tuple
 
@@ -7,6 +8,7 @@ from accelerate.utils import find_batch_size, send_to_device
 from safetensors.torch import load_file, save_file
 from torch import autocast, nn
 from torch.cuda.amp import GradScaler
+from torch.profiler import ProfilerActivity, profile, schedule, tensorboard_trace_handler
 from torch.utils import data
 from tqdm import tqdm
 
@@ -202,12 +204,33 @@ def fit_covariance_matrices_with_loader(
         gradient_scale = 1.0 / scaler.get_scale()
         set_gradient_scale(model=model, gradient_scale=gradient_scale)
 
+    profile_dir = os.environ.get("KRONFLUENCE_PROFILE_DIR")
+    if profile_dir:
+        os.makedirs(profile_dir, exist_ok=True)
+        trace_handler = tensorboard_trace_handler(
+            dir_name=profile_dir, worker_name="rank_0", use_gzip=True
+        )
+        my_schedule = schedule(wait=0, warmup=0, active=4, repeat=1)
+        prof = profile(
+            activities=[ProfilerActivity.CPU, ProfilerActivity.CUDA],
+            on_trace_ready=trace_handler,
+            schedule=my_schedule,
+            record_shapes=True,
+            with_stack=True,
+            profile_memory=True,
+            with_modules=True,
+        )
+    else:
+        prof = None
+
     with tqdm(
         total=len(loader),
         desc="Fitting covariance matrices",
         bar_format=TQDM_BAR_FORMAT,
         disable=not state.is_main_process or disable_tqdm,
     ) as pbar:
+        if prof is not None:
+            prof.__enter__()
         for index, batch in enumerate(loader):
             batch = send_to_device(batch, device=state.device)
 
@@ -236,6 +259,10 @@ def fit_covariance_matrices_with_loader(
             del loss
             total_steps += 1
             pbar.update(1)
+            if prof is not None:
+                prof.step()
+        if prof is not None:
+            prof.__exit__(None, None, None)
 
     if state.use_distributed:
         synchronize_modules(model=model, tracked_module_names=tracked_module_names)
diff --git a/kronfluence/module/linear.py b/kronfluence/module/linear.py
index 2fcafec..1a9a63a 100644
--- a/kronfluence/module/linear.py
+++ b/kronfluence/module/linear.py
@@ -36,7 +36,7 @@ class TrackedLinear(TrackedModule, module_type=nn.Linear):
             flattened_attention_mask = rearrange(tensor=self.attention_mask, pattern="b ... -> (b ...) 1")
             flattened_activation.mul_(flattened_attention_mask)
 
-        if self.original_module.bias is not None:
+        if self.original_module.bias is not None and self.factor_args.include_bias:
             append_term = flattened_activation.new_ones((flattened_activation.size(0), 1), requires_grad=False)
             if flattened_attention_mask is not None:
                 append_term.mul_(flattened_attention_mask)
@@ -48,13 +48,18 @@ class TrackedLinear(TrackedModule, module_type=nn.Linear):
     def get_flattened_gradient(self, output_gradient: torch.Tensor) -> Tuple[torch.Tensor, Union[torch.Tensor, int]]:
         flattened_gradient = rearrange(tensor=output_gradient, pattern="b ... d_out -> (b ...) d_out")
         if self.attention_mask is not None and flattened_gradient.size(0) == self.attention_mask.numel():
-            count = self.attention_mask.sum()
+            # Zero-out gradients at masked positions to match activation masking.
+            # Without this, intermediate-layer gradients at prompt positions (non-zero
+            # due to attention backprop) contaminate the gradient covariance.
+            flattened_attention_mask = rearrange(tensor=self.attention_mask, pattern="b ... -> (b ...) 1")
+            flattened_gradient = flattened_gradient * flattened_attention_mask
+            count = flattened_attention_mask.sum()
         else:
             count = flattened_gradient.size(0)
         return flattened_gradient, count
 
     def _flatten_input_activation(self, input_activation: torch.Tensor) -> torch.Tensor:
-        if self.original_module.bias is not None:
+        if self.original_module.bias is not None and self.factor_args.include_bias:
             shape = list(input_activation.size()[:-1]) + [1]
             append_term = input_activation.new_ones(shape, requires_grad=False)
             input_activation = torch.cat([input_activation, append_term], dim=-1)
